
#include "MenuUtils.h"

namespace MastWarden {

  class MainMenu : public MastTDE::MenuContainer {
  public:
    virtual MENU* GetRawMenu() override;
    virtual void CompileMenu() = 0;
    virtual const char** GetItemsArray();
    virtual int GetFirstColor() override;
  private:
    std::vector<MastTDE::Menu> m_Menus;
  }
  
}
